import {StyleSheet} from 'react-native';

export const CommonStyles = StyleSheet.create({
    fullFlex: {
        flex: 1,
    },
    fullCenter: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});
